1. la carpeta a utilizar es portfolio
2. en ella se encontrara el archivo sql de la base de datos llamador "nosviveref", se debe crear una base de datos nueva en el phpmyadmin con el nombre "nosviveref" y proceder a importar el archivo
deberia quedar creada una tabla personas
3. la pagina de inicio esta en la carpeta portfolio y esta es llamada index.html esto se abre con el local host